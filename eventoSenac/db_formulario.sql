create database formulario;
use formulario;

create table usuarios (id int auto_increment primary key, nome varchar(45), email varchar(45), telefone varchar(15),idade varchar(2), sexo varchar(30), curso varchar(100));
select * from usuarios;

#drop database formulario;

